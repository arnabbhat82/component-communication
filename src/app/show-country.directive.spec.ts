import { ShowCountryDirective } from './show-country.directive';

describe('ShowCountryDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowCountryDirective();
    expect(directive).toBeTruthy();
  });
});
